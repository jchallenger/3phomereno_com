console.log("Literally none of this is ready...");


var my_key

$(document).ready(function(){
    $.ajax({
        url: base_url + 'respond/'
    });
    // check if session already started
    // request public key from server

    // create private key

});
function login(){

}
